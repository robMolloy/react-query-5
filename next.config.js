/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  pageExtensions: ["route.tsx", "route.ts", "page.tsx", "page.ts", "tsx", "ts"],
};

module.exports = nextConfig;
